[package]
name = "artifact"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
core_types = { path = "../core_types", features = [ "ssr" ] }

bytes.workspace = true
eyre.workspace = true
object_store = { version = "0.10", features = [ "aws" ] }
